# Web framework — FastAPI (MIT), Uvicorn ASGI server (BSD)
fastapi==0.111.0
uvicorn[standard]==0.29.0

# LLM and Agent Orchestration — Groq SDK (Apache 2.0), LangChain (MIT)
groq==0.9.0
langchain==0.2.16
langchain-groq==0.1.9

# Configuration and Validation — Pydantic (MIT), pydantic-settings (MIT)
pydantic==2.7.1
pydantic-settings==2.2.1

# Database — SQLAlchemy ORM (MIT), Alembic migrations (MIT)
sqlalchemy==2.0.30
alembic==1.13.1

# Caching — Redis (BSD)
redis==5.0.4

# Environment — python-dotenv (BSD)
python-dotenv==1.0.1

# HTTP client — httpx (BSD)
httpx==0.27.0

# RAG / Semantic Search — fastembed (MIT, ONNX-based, Windows-safe), numpy (BSD)
fastembed==0.3.6
numpy>=1.24.0

# Testing — pytest (MIT), pytest-asyncio (Apache 2.0)
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0

python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
